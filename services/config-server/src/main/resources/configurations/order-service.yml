spring:
  datasource:
    url: jdbc:postgresql://localhost:5001/order
    username: mustopha
    password: ayinde_99
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    producer:
      bootstrap-servers: localhost:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.type.mapping: orderConfirmation:com.abuhanaan.ecommerce.kafka.OrderConfirmation

  application:
    config:
      customer-url: http://localhost:8090/api/v1/customers
      product-url: http://localhost:8050/api/v1/products
      payment-url: http://localhost:8060/api/v1/payments

server:
    port: 8070




